<window id="1147" type="dialog">
    
    <!-- Delay -->
    <visible>System.IdleTime(10) + !String.IsEmpty(Window(home).Property(nowplayingautoshowtimer)) + Skin.HasSetting(extended.nowplaying) + Window.IsVisible(Home) + [Player.HasMedia | Player.Playing] + Skin.HasSetting(home.nowplaying.controls) + Skin.HasSetting(home.nowplaying.controls.auto.show)</visible>
    
    <!-- Close Controls -->
    <onload>ClearProperty(nowplayingautoshowtimer,home)</onload>
    <onunload>SetFocus(300)</onunload>
    <controls/>
</window>